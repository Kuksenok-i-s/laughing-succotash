# Copy to .env in this directory. Compose reads it automatically, so `docker compose ps`,
# `logs` and `restart` work without exporting anything into the shell.
#
# Generate the secret once:
#   openssl rand -hex 32
SEARXNG_SECRET=
