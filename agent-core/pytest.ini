[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
filterwarnings =
    error::DeprecationWarning:agent_core.*
markers =
    real_cursor: hits the real Cursor Agent; skipped unless PA_REAL_CURSOR_TESTS=1
    slow: takes more than a few seconds
