# Agent Core runtime dependencies (Intel Mac mini).
#
# The shared wire protocol lives in this repository and is installed from a path:
#   pip install -e ../packages/pa-protocol
#
# Whisper is intentionally not here — see requirements-stt.txt. It pulls CTranslate2, which is a
# large platform-specific wheel, and the Core is developed and tested on machines that do not need
# it.

pydantic>=2.7
pydantic-settings>=2.2
aiohttp>=3.9
websockets>=12.0
python-dateutil>=2.9
tzdata>=2024.1
