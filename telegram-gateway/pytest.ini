[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
filterwarnings =
    error::DeprecationWarning:telegram_gateway.*
markers =
    slow: takes more than a few seconds
